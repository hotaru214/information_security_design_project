可以把题目 3 理解成一句话：

> **你们要做一个“小型安全取证/攻击溯源平台”：把不同机器产生的安全数据收进来，自动整理、关联，然后告诉用户“攻击者什么时候从哪里进来，做了什么，经过哪些机器，最后干了什么”。**

所以最终成果应该是：

> **一个可运行的软件系统 + 一套安全分析能力 + 一组实验数据/靶场验证 + 报告与 PPT。**

不是单纯做网页，也不是单纯写安全分析报告。

---

# 1. 先用零基础理解这个题到底在干什么

假设你们有一个小公司网络：

```text
攻击者电脑
    ↓
Web服务器
    ↓
办公电脑
    ↓
核心服务器
    ↓
C2服务器
```

攻击发生之后，每台机器都会留下“脚印”。

比如 Web 服务器可能留下：

```text
13:01  1.2.3.4 访问 /login
13:02  某进程被启动
13:03  Web服务器主动连接 192.168.1.20
```

Windows 电脑可能留下：

```text
13:04  用户 Alice 登录
13:05  powershell.exe 被启动
13:06  powershell.exe 访问某个文件
```

网络设备又可能看到：

```text
Web服务器 → 办公电脑
办公电脑 → 核心服务器
核心服务器 → C2服务器
```

单独看，每条信息都很碎。

题目 3 要做的事情就是：

```text
大量零散数据
       ↓
整理成统一格式
       ↓
找到彼此之间的关系
       ↓
还原攻击时间线
       ↓
重建攻击链
       ↓
判断攻击者做过什么
```

任务书明确要求处理三类数据：

* Windows/Linux 主机日志
* Windows/Linux 主机行为
* 企业网络流量

还要求做事件关联、ATT&CK 攻击链识别、横向移动路径和数据外传路径分析。

所以它本质上是：

> **数据工程 + 后端系统 + 网络安全分析。**

这正是为什么我觉得它比题 1、题 2 更适合你们。

---

# 2. 那我们到底需不需要做前端？

## 建议：做，但一定要轻

任务书的题名本身就是：

> “恶意攻击行为溯源分析**系统设计与实现**”

而最终考核又明确包括**程序源代码、PPT、截屏录像**，现场还要进行“产品展示”。

所以只有：

```text
几个 Python 脚本
+
一份分析报告
```

是不理想的。

最好做成：

```text
          浏览器
            │
            ▼
┌─────────────────────┐
│   安全溯源分析平台    │
├─────────────────────┤
│ 攻击事件列表          │
│ 攻击时间线            │
│ 攻击链关系图          │
│ ATT&CK 技术映射       │
│ LLM 分析报告          │
└─────────────────────┘
            │
            ▼
       FastAPI 后端
            │
   ┌────────┼────────┐
   ▼        ▼        ▼
日志解析   流量解析   关联分析
   │        │        │
   └────────┼────────┘
            ▼
          数据库
```

但注意：

> **前端不是这次项目的主角。**

千万不要两个人花两天时间调 CSS、做登录页面、设计炫酷动画。

前端只负责把安全分析结果**展示出来**。

甚至 React/Vue 都不是必须。

如果时间只有 3 天，我反而建议：

**FastAPI + 简单 HTML/JS + ECharts**

就够了。

---

# 3. 后端具体要干什么？

这是你们最熟悉，也应该投入最多人力的地方。

整个程序可以压成 5 个模块：

```text
① 数据导入
       ↓
② 数据解析
       ↓
③ 数据标准化
       ↓
④ 攻击事件关联
       ↓
⑤ 分析结果展示
```

例如原始数据来自：

```text
Windows Event Log
Linux 日志
网络流量
```

不同格式乱七八糟。

你们后端统一成：

```json
{
  "timestamp": "2026-09-08 13:05:02",
  "host": "web-server",
  "event_type": "process_start",
  "user": "www-data",
  "process": "bash",
  "src_ip": "192.168.1.10",
  "dst_ip": "192.168.1.20",
  "description": "abnormal process execution"
}
```

然后这些事件就容易分析了。

例如：

```text
13:01
攻击机 → Web Server

13:02
Web Server 启动异常进程

13:04
Web Server → Office PC

13:06
Office PC 登录 Core Server

13:08
Core Server → C2
```

最后程序自动生成：

```text
Initial Access
      ↓
Execution
      ↓
Lateral Movement
      ↓
Collection
      ↓
Command & Control
```

这就是你们项目最核心的成果。

---

# 4. “ATT&CK”是什么？

你们之后会频繁见到这个词。

MITRE ATT&CK 可以先理解成：

> **一本给网络攻击行为分类的“字典”。**

比如攻击者：

```text
钓鱼
```

属于一种攻击技术。

运行 PowerShell：

```text
Command and Scripting Interpreter
```

获取密码：

```text
Credential Access
```

从 A 机器跳到 B 机器：

```text
Lateral Movement
```

所以你们系统最后可以说：

```text
检测到事件：

powershell.exe
    ↓
映射：
Execution
    ↓
MITRE ATT&CK Technique T1059
```

这一下就有很明显的网络安全课程设计味道。

---

# 5. 大模型 Agent 在哪里？

任务书题目 3 本身明确提到了“大模型多智能体协调技术”。

所以建议保留，但是**千万不要第一天就做 Multi-Agent 大框架**。

你们可以简单设计三个逻辑 Agent：

```text
日志分析 Agent
     │
     ├── 找异常登录
     ├── 找异常进程
     └── 找异常文件访问

网络分析 Agent
     │
     ├── 分析异常连接
     └── 分析可疑外联

溯源 Agent
     │
     └── 综合事件，形成攻击链
```

实际上后台甚至可以只是普通 Python service + LLM API。

答辩时从架构上称作：

```text
Host Analysis Agent
Network Analysis Agent
Correlation Agent
```

即可。

**真正关键的不是你用了 LangChain 还是自己写 Agent，而是最终能不能给出正确、有证据的攻击链。**

---

# 6. 6 个人，我建议这样分

你们只有 **3 天**。

因此我不建议：

```text
前端2人
后端2人
安全2人
```

这种传统分工。

因为会产生巨大的等待和接口对接成本。

我建议按**功能链路**拆。

| 成员           | 主任务                | 最终成果                 |
| ------------ | ------------------ | -------------------- |
| **A 组长/集成**  | 后端架构、数据库、API、代码合并  | FastAPI 主工程          |
| **B 数据①**    | Windows/Linux 日志解析 | host event parser    |
| **C 数据②**    | 网络流量解析             | network event parser |
| **D 安全分析**   | 事件关联、ATT&CK 映射     | correlation engine   |
| **E 靶场/数据**  | 准备攻击数据、靶场、验证       | 测试环境 + 数据            |
| **F 展示/LLM** | 前端可视化、Agent 分析     | Web Dashboard        |

但是每个人还有一个副任务：

```text
每个人负责自己模块对应的报告章节。
```

不能让 F 一个人第三天晚上写全部报告。

---

# 7. 六个人分别具体干什么

## A：项目负责人 + Backend

这个人最好是你们组里后端最熟的人。

负责：

```text
FastAPI
SQLite / PostgreSQL
项目结构
API
数据库 Schema
模块集成
```

可以设计几张最简单的表：

```text
hosts
events
network_connections
attack_steps
analysis_tasks
```

提供：

```text
POST /api/events/import
GET  /api/events

POST /api/analysis
GET  /api/attack-chain

GET  /api/hosts
GET  /api/statistics
```

A 不负责安全算法，只负责**让大家的数据能在一个系统里流动起来**。

---

# 8. B：主机日志

负责搞明白：

> “Windows/Linux 机器留下什么日志？”

不要自己实现内核监控器。

任务书虽然写了系统调用、文件、进程、内存等主机行为分析， 但你们三天时间，应该优先利用现成数据/工具。

例如了解：

**Windows：**

```text
Windows Event Log
Sysmon
```

**Linux：**

```text
auth.log
auditd
```

然后负责把这些东西转换成统一结构。

例如：

```text
原始 Windows Event
        ↓
parser
        ↓
{
 timestamp,
 host,
 user,
 process,
 event_type
}
```

B 的成果不是“会用 Sysmon”。

而是：

> **项目能导入主机日志，并生成结构化安全事件。**

---

# 9. C：网络流量

负责：

> “这些机器之间发生了什么网络通信？”

可以学习：

```text
PCAP
IP
TCP
HTTP
DNS
```

工程上不建议自己写完整 TCP parser。

我的建议是利用成熟工具，比如 **Zeek** 或类似流量分析工具，把 PCAP 转换成结构化日志。

注意，这是我的工程建议，不是任务书指定的软件。

最后输出类似：

```json
{
  "timestamp": "...",
  "src_ip": "10.0.0.1",
  "dst_ip": "10.0.0.5",
  "dst_port": 4444,
  "protocol": "TCP"
}
```

然后提交给 A 的后端。

---

# 10. D：整个题最核心的人

D 负责：

## 攻击事件关联

比如数据库存在：

```text
事件1
攻击IP → Web

事件2
Web启动异常进程

事件3
Web → Office PC

事件4
Office PC → Server
```

D 写规则，把它们连接：

```text
1 → 2 → 3 → 4
```

同时研究最基本的：

```text
MITRE ATT&CK
Initial Access
Execution
Persistence
Privilege Escalation
Lateral Movement
Collection
Command & Control
```

最后生成：

```json
{
  "stage": "Lateral Movement",
  "source_host": "web",
  "target_host": "office",
  "evidence": [...]
}
```

如果让我选这个项目最需要认真做好的模块：

> **就是 D。**

因为没有这个东西，你们只是一个“日志查看器”。

有了它，你们才是：

> **攻击溯源系统。**

---

# 11. E：安全实验/靶场

这个人最像本次的“安全实践担当”。

任务书的完整要求其实相当重：

> 要搭建不少于 8 个节点，包括攻击节点、C2、防火墙、Web、Email、交换机、办公计算机和核心服务器，还要构建完整入侵链。

### 我要特别提醒你们

**对六个安全零基础学生，3 天时间严格完整实现这一部分，非常困难。**

所以必须平行进行。

E 第一天下午就要开始研究环境，不能等系统写完。

可以构造逻辑：

```text
Attacker
│
Firewall
│
Web Server
│
Switch
├────── Office PC
├────── Mail Server
└────── Core Server
             │
             C2
```

不一定意味着：

> 8 台沉重的虚拟机。

可以考虑：

```text
VM
+
Docker container
+
虚拟网络
```

组成 8 个逻辑节点。

但这一点是否完全符合老师对“节点”的验收口径，**任务书没有进一步解释**。因此你们最好尽快向老师确认容器节点是否认可。

---

# 12. F：前端 + LLM

F 做非常轻量的界面。

我会限定只有四页。

## 首页 Dashboard

```text
分析事件：3267
异常事件：42
涉及主机：8
攻击阶段：5
```

## 时间线

```text
13:01 Initial Access
13:03 Execution
13:05 Lateral Movement
13:08 Collection
13:10 C2
```

## 攻击链

```text
Attacker
   ↓
Web
   ↓
Office
   ↓
Core Server
   ↓
C2
```

用 ECharts 做即可。

## 分析报告

LLM 输出：

```text
攻击路径：
...

关键证据：
...

ATT&CK 映射：
...

风险说明：
...
```

这已经足够现场展示。

---

# 13. 三天怎么安排

这是最重要的地方。

你们不能：

> Day 1 学安全
> Day 2 写代码
> Day 3 拼起来

一定完蛋。

六个人要**从第一小时开始并行**。

---

## Day 1：让整条链第一次跑通

目标不是漂亮。

目标是今晚必须做到：

```text
安全数据
↓
后端
↓
数据库
↓
简单分析
↓
浏览器
```

### 上午

A：

```text
FastAPI骨架
数据库
event数据模型
基础接口
Git仓库
```

B：

```text
学习主机日志
找/准备样例日志
开始 parser
```

C：

```text
学习 PCAP / Zeek
准备流量样例
开始 parser
```

D：

```text
学习 ATT&CK
定义统一事件模型
定义第一版关联规则
```

E：

```text
设计8节点拓扑
准备实验环境
寻找/准备攻击数据
```

F：

```text
前端骨架
Dashboard
Timeline 页面
```

### 下午到晚上

完成第一次：

```text
sample data
→ Parser
→ events DB
→ API
→ 页面
```

即使只有 20 条假数据都可以。

**Day 1 必须看到页面上出现安全事件。**

---

# 14. Day 2：做真正的安全能力

这是核心开发日。

B：

```text
完成 host parser
异常登录
异常进程
```

C：

```text
完成 network parser
网络连接
异常外联
```

D：

```text
完成 attack correlation
ATT&CK mapping
timeline
```

E：

```text
靶场
产生实验数据
验证日志和流量
```

F：

```text
攻击时间线
攻击链图
LLM分析
```

A：

```text
整合所有模块
API
数据库
异常处理
```

Day 2 晚上的目标：

> **导入一套数据 → 点击“开始分析” → 得到一条攻击链。**

如果做到这里，项目实际上已经活了。

---

# 15. Day 3：不要再扩功能

第三天最大的诱惑是：

> “我们是不是还能加一个 XXX？”

不要。

第三天只干：

```text
Bug
测试
演示
报告
PPT
录像
```

上午：

* 跑完整 Demo
* 修崩溃
* 固定测试数据
* 完成靶场验证
* 保存截图

下午：

* 写报告
* PPT
* 测试报告
* 分工文件
* 安装说明

晚上：

连续完整演示至少 3 次：

```text
启动服务
↓
导入数据
↓
开始分析
↓
攻击链出现
↓
打开事件
↓
展示证据
↓
ATT&CK映射
↓
LLM总结
```

目标：

> **断网都尽量能演示核心功能。**

因为 9 月 12 日现场只有 **15 分钟 PPT + 产品展示**。

---

# 16. 三天版本，哪些功能必须有？

我给你们划一道非常重要的线。

## 🔴 必须完成

```text
✓ 主机日志导入
✓ 网络数据导入

✓ 统一事件格式

✓ 后端数据库

✓ 攻击事件时间线

✓ 至少一条攻击链关联

✓ ATT&CK映射

✓ 简单Web展示

✓ 一套实验/数据集验证

✓ 报告
✓ PPT
✓ 演示
```

---

## 🟡 有时间再做

```text
○ LLM Agent
○ 自动生成安全报告
○ 攻击关系图
○ 多次攻击识别
○ 风险评分
```

---

## ⚪ 三天内别碰

例如：

```text
× 自己写内核监控程序
× 自己写 IDS
× 自己写 PCAP TCP 重组
× 复杂机器学习异常检测
× 真正 APT 组织归因
× 完整 SIEM
× 特别复杂的 Multi-Agent
```

任务书确实提到了内存行为、隐蔽信道、APT 匹配等非常大的内容。

但 3 天版本应该追求：

> **先有完整闭环，再扩功能覆盖。**

---

# 17. 最终你们应该拿出什么东西？

这可以分成 **作品** 和 **提交材料** 两部分。

## 一、软件作品

至少应该存在：

```text
project/
│
├── backend/
│   ├── parsers/
│   ├── correlation/
│   ├── attack_mapping/
│   └── api/
│
├── frontend/
│
├── data/
│   ├── host_logs/
│   └── network_logs/
│
├── scripts/
│
├── requirements.txt
│
└── README.md
```

运行之后浏览器可以打开：

```text
http://localhost:xxxx
```

看到你们的：

**恶意攻击行为溯源分析系统**

---

# 18. 二、安全实验成果

至少准备一套完整案例，例如：

> **Case 01：企业内网攻击行为溯源**

输入：

```text
Windows日志
Linux日志
网络流量
```

系统输出：

```text
攻击入口
↓
异常执行
↓
横向移动
↓
核心服务器
↓
C2

时间线

攻击节点

证据事件

ATT&CK分类
```

这个东西会成为整个答辩的灵魂。

---

# 19. 三、任务书明确要求提交的材料

这部分不要漏。

官方明确要求打包：

1. **任务分工说明**
2. **作品技术原理介绍**
3. **概要设计报告**
4. **详细设计报告**
5. **测试分析报告**
6. **程序编译和安装使用文档**
7. **程序源代码**
8. **PPT**
9. **截屏录像**



所以最终不是交一个 GitHub 项目就结束。

---

# 20. 你们最终答辩应该讲成什么故事？

不要从：

> “我们用了 FastAPI、Vue、SQLite……”

开始。

老师并不主要关心这些。

应该讲：

### ① 问题

企业遭到攻击后：

```text
日志分散
数据格式不同
事件数量巨大
攻击路径难以还原
```

### ② 我们的方法

```text
多源数据采集
↓
统一事件模型
↓
异常事件识别
↓
跨主机事件关联
↓
ATT&CK映射
↓
攻击链重建
```

### ③ Demo

比如：

```text
这里有一组攻击数据。

系统检测到：

13:01 外部主机访问 Web
13:03 Web 服务器出现异常进程
13:05 Web 服务器访问 Office PC
13:07 Office PC 访问 Core Server
13:10 Core Server 连接 C2
```

于是系统自动重建：

```text
Attacker
  ↓
Web
  ↓
Office
  ↓
Core
  ↓
C2
```

### ④ 证据

点击某节点：

```text
来源：Windows Event
时间：13:05
Process：xxx
Source IP：xxx
```

### ⑤ ATT&CK

```text
Execution
Lateral Movement
Command & Control
```

### ⑥ 最后总结

> 系统通过主机日志、主机行为及网络流量等多源信息进行融合，实现攻击事件关联以及攻击路径重建。

这和任务书描述的核心目标是高度一致的。

---

## 最后一个非常重要的判断

如果是：

**6 个人 + 3 天 + 大部分安全零基础**

那么题目 3 依然能做，但你们要把它当成：

> **“后端数据分析系统中加入安全领域逻辑”**

而不是：

> **“三天成为安全攻防专家”。**

我建议人力大概是：

**4 人做核心数据/后端/安全分析，1 人专职实验环境，1 人做前端展示和 LLM，所有人共同写文档。**

而项目成败最关键的不是页面漂不漂亮，而是最终能不能非常清楚地展示这条链：

> **原始日志/流量 → 标准化事件 → 事件关联 → ATT&CK → 攻击时间线/攻击链 → 证据。**

只要这条链真正跑起来，你们就抓住了题目 3 的核心。
