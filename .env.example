# LLM 分析服务配置模板（成员F）——复制为 .env 并填入真实值；.env 已被 .gitignore 排除，严禁提交
# OpenAI 兼容协议：换厂商只改这里（DeepSeek/千问/GLM/Kimi 均兼容）
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-xxx
LLM_MODEL=deepseek-chat
# 可选：请求超时秒数（默认 30）
# LLM_TIMEOUT_SECONDS=30
